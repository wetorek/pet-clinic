spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
      temp:
        use_jdbc_metadata_defaults: false
  datasource:
    url: jdbc:h2:file:./src/test/resources/db/db;AUTO_SERVER=true
    username: sa
    password: sa